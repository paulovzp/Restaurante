/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import { Prato } from './prato';

describe('Prato', () => {
    it('should create an instance', () => {
        expect(new Prato()).toBeTruthy();
    });
});