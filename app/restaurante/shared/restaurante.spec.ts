/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import { Restaurante } from './restaurante';

describe('Restaurante', () => {
    it('should create an instance', () => {
        expect(new Restaurante()).toBeTruthy();
    });
});